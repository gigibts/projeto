<div class="col mt-2">

<div class="card text-dark  text-center">

<img src="@Model.ImagemPequenaUrl" class="card-img-top" alt="...">

                <div class="card-body">

                    <h5 class="card-title">    
                        <a class="text-black" asp-controller="Item" asp-action="Details" asp-route-itemId="@Model.ItemId">@Model.Nome</a>
                    </h5>

                     <h6>Preço : @Model.Preco.ToString("c")</h6>

                    <p class="card-text">@Model.DescricaoCurta</p>

                    <a class="btn corlaranja d-block col-8 mx-auto" id="cartButton" asp-controller="Carrinho"
                    asp-action="AdicionarItemNoCarrinhoCompra" asp-route-itemId="@Model.ItemId">Incluir no Carrinho
                    </a>

                </div>
</div>
</div>